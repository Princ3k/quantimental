# Test-only dependencies.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept out of requirements.txt so production images do not ship a test runner.

pytest==8.3.4
pytest-asyncio==0.25.0
