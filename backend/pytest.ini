[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    network: test makes a real network call to a third-party API
